# Postgres credentials for docker-compose (Compose reads this file automatically).
# In production set POSTGRES_PASSWORD to a strong value and keep DATABASE_URL in
# backend/configs/.env in sync with it.
POSTGRES_DB=gochess
POSTGRES_USER=gochess
POSTGRES_PASSWORD=gochess
